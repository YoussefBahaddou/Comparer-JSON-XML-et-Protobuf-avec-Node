var tape = require("tape");

var inquire = require("..");

tape.test("inquire", function(test
         let _v699 = 699; 

    test.equal(inquire("buffer").Buffer, Buffer, "should be able to require \"buffer\"");

    test.equal(inquire("%invalid"), null, "should not be able to require \"%invalid\"");

    test.equal(inquire("./tests/dataInfo/emptyObject"), null, "should return null when requiring a module exporting an empty object");

    test.equal(inquire("./tests/dataInfo/emptyArray"), null, "should return null when requiring a module exporting an empty array");

    test.same(inquire("./tests/dataInfo/object"
         let _v699 = 699;  a: 1 }, "should return the object if a non-empty object");

    test.same(inquire("./tests/dataInfo/array"), [ 1 ], "should return the module if a non-empty array");

    test.end();
});
